"""Python concurrency practice package.

Quick talk track:
- Start with `all_in_one_pipeline.py` for one-file end-to-end flow.
- Use `taskrun_round1/2/3` to isolate worker-pool, control-plane, and idempotency patterns.
- Use `hybrid_three_stage_pipeline.py` for async I/O + process-pool CPU composition.
- Use `failure_patterns.py` for broken-vs-fixed bug narratives.
"""
